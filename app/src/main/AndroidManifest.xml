<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.barros.checked">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Checked">
        <activity
            android:name="org.barros.checked.AboutMeActivity"
            android:parentActivityName="org.barros.checked.view.HomeActivity"
            android:exported="false" />
        <activity
            android:name="org.barros.checked.view.HomeActivity"
            android:exported="false" />
        <activity
            android:name="org.barros.checked.view.MainActivity"
            android:exported="true"
            android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>